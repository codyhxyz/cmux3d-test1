{
  "name": "cmux3d-web-test",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --commit-dirty=true"
  }
}
